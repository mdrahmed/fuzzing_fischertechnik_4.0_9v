In the sub-folders, `main.cpp` is to create simulation for specific controllers. And `fuzz_<controller-name>.cpp` is for fuzzing the simulation of the controller-name provided.
Type `quit` to stop FSM.
